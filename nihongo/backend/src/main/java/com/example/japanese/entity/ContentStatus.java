package com.example.japanese.entity;

/**
 * Shared publication status for learning content (Level, Lesson - and Exam
 * in a later phase, which the requirements doc gives the same DRAFT /
 * PUBLISHED / ARCHIVED values, section 17.1).
 */
public enum ContentStatus {
    DRAFT,
    PUBLISHED,
    ARCHIVED
}
